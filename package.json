{
  "name": "brain-injury-matters-html",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "sass:watch": "sass --watch assets/scss/style.scss:assets/css/style.min.css --style=compressed --source-map",
    "sass:build": "sass assets/scss/style.scss:assets/css/style.min.css --style=compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "1.103.0"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
